# 栈

以下是 Java 中常见数据结构的常用方法对比表格，涵盖初始化、增删改、计算长度等核心操作：

| 数据结构                          | 初始化方式                                                   | 增加元素方法                                                 | 删除元素方法                                                 | 修改元素方法                                                 | 计算长度方法         | 其他常用方法                                                 |
| --------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | -------------------- | ------------------------------------------------------------ |
| 数组（Array）                     | `int[] arr = new int[5];` `int[] arr = {1,2,3};`             | 无直接方法（长度固定，需创建新数组复制）                     | 无直接方法（需创建新数组复制）                               | `arr[index] = value;`（通过索引赋值）                        | `arr.length`（属性） | 数组拷贝：`System.arraycopy(src, srcPos, dest, destPos, length)` |
| ArrayList                         | `List<Integer> list = new ArrayList<>();` `List<Integer> list = new ArrayList<>(10);` | `add(E e)`（尾插） `add(int index, E e)`（指定位置插入）     | `remove(int index)`（按索引删） `remove(Object o)`（按元素删） | `set(int index, E e)`（替换指定位置元素）                    | `size()`             | `get(int index)`（获取元素） `contains(Object o)`（判断是否包含） |
| LinkedList                        | `List<Integer> list = new LinkedList<>();` `Queue<Integer> queue = new LinkedList<>();` | `add(E e)`（尾插） `addFirst(E e)`（头插） `addLast(E e)`（尾插） | `removeFirst()`（删头） `removeLast()`（删尾） `remove(int index)`（按索引删） | `set(int index, E e)`（替换指定位置元素）                    | `size()`             | `getFirst()`/`getLast()`（获取首尾元素） `poll()`（出队，删头） |
| StringBuffer                      | `StringBuffer sb = new StringBuffer();` `StringBuffer sb = new StringBuffer("init");` | `append(E e)`（尾加，支持多种类型） `insert(int index, E e)`（指定位置插入） | `delete(int start, int end)`（删除 [start,end) 区间） `deleteCharAt(int index)`（删指定位置） | `replace(int start, int end, String str)`（替换区间） `setCharAt(int index, char c)` | `length()`           | `reverse()`（反转） `toString()`（转为 String）              |
| Stack                             | `Stack<Integer> stack = new Stack<>();`                      | `push(E item)`（入栈，压入栈顶）                             | `pop()`（出栈，删除并返回栈顶） `remove(int index)`（按索引删） | 无直接方法（需先 pop 再 push 新值）                          | `size()`             | `peek()`（查看栈顶，不删除） `empty()`（判断是否为空）       |
| Queue（接口，以 LinkedList 实现） | `Queue<Integer> queue = new LinkedList<>();`                 | `add(E e)`（入队，尾插，满时抛异常） `offer(E e)`（入队，尾插，满时返回 false） | `remove()`（出队，删头，空时抛异常） `poll()`（出队，删头，空时返回 null） | 无直接方法（队列通常不支持随机修改）                         | `size()`             | `element()`（查看队头，空时抛异常） `peek()`（查看队头，空时返回 null） |

## 字符串解码 lc394

给定一个经过编码的字符串，返回它解码后的字符串。

编码规则为: `k[encoded_string]`，表示其中方括号内部的 `encoded_string` 正好重复 `k` 次。注意 `k` 保证为正整数。

你可以认为输入字符串总是有效的；输入字符串中没有额外的空格，且输入的方括号总是符合格式要求的。

此外，你可以认为原始数据不包含数字，所有的数字只表示重复的次数 `k` ，例如不会出现像 `3a` 或 `2[4]` 的输入。

测试用例保证输出的长度不会超过 `105`。

**示例 1：**

```
输入：s = "3[a]2[bc]"
输出："aaabcbc"
```

**示例 2：**

```
输入：s = "3[a2[c]]"
输出："accaccacc"
```

**提示：**

- `1 <= s.length <= 30`
- `s` 由小写英文字母、数字和方括号 `'[]'` 组成
- `s` 保证是一个 **有效** 的输入。
- `s` 中所有整数的取值范围为 `[1, 300]` 

```java
class Solution {
    public String decodeString(String s) {
        // 辅助栈，用于存储倍数和中间结果
        Stack<Object> stack = new Stack<>();
        // 用于构建结果的字符串
        StringBuilder res = new StringBuilder();
        // 用于计算倍数的变量
        int multi = 0;
        
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                // 处理多位数情况，例如"123"需要解析为123
                multi = multi * 10 + (c - '0');
            } else if (c == '[') {
                // 遇到左括号，将当前倍数和结果入栈，并重置
                stack.push(multi);
                stack.push(res.toString());
                multi = 0;          // 重置倍数
                res = new StringBuilder();  // 重置当前结果，准备处理括号内内容
            } else if (c == ']') {
                // 遇到右括号，出栈并拼接字符串
                String lastRes = (String) stack.pop();  // 上一个[前的字符串
                int curMulti = (int) stack.pop();        // 当前[对应的倍数
                
                // 拼接：上一个结果 + 当前结果重复curMulti次
                StringBuilder temp = new StringBuilder(lastRes);
                for (int i = 0; i < curMulti; i++) {
                    temp.append(res);
                }
                res = temp;  // 更新当前结果为拼接后的结果
            } else {
                // 普通字符直接添加到当前结果
                res.append(c);
            }
        }
        
        return res.toString();
    }
}
```

## 有效的括号

给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s` ，判断字符串是否有效。

有效字符串需满足：

1. 左括号必须用相同类型的右括号闭合。
2. 左括号必须以正确的顺序闭合。
3. 每个右括号都有一个对应的相同类型的左括号。

**示例 1：**

**输入：**s = "()"

**输出：**true

**示例 2：**

**输入：**s = "()[]{}"

**输出：**true

**示例 3：**

**输入：**s = "(]"

**输出：**false

```java
class Solution {
    public boolean isValid(String s) {
        //思路：利用栈，是左括号就加入栈，遇到右括号就出栈，如果遇到出栈的与入栈的不匹配就直接flase，最后栈为空则全部匹配

        //如果s长度不为偶数，直接false
        if (s.length() % 2 != 0){return false;}

        Stack<Character> stack = new Stack<Character>();
        for(int i = 0;i < s.length();i++){
            char cur = s.charAt(i);
            //如果为左括号
            if (cur == '(' || cur == '[' || cur == '{') {
                stack.push(cur);
                continue;//重点，易错
            }
            //如果为右括号
            if (!stack.isEmpty()){
                char c = stack.pop();
                if(cur == ')' && c != '(') return false;
                else if (cur == ']' && c != '[') return false;
                else if (cur == '}' && c != '{') return false;
            }else return false;
        }
        return stack.isEmpty();
    }
}
```

## 最小栈

设计一个支持 `push` ，`pop` ，`top` 操作，并能在常数时间内检索到最小元素的栈。

实现 `MinStack` 类:

- `MinStack()` 初始化堆栈对象。
- `void push(int val)` 将元素val推入堆栈。
- `void pop()` 删除堆栈顶部的元素。
- `int top()` 获取堆栈顶部的元素。
- `int getMin()` 获取堆栈中的最小元素。

**示例 1:**

```
输入：
["MinStack","push","push","push","getMin","pop","top","getMin"]
[[],[-2],[0],[-3],[],[],[],[]]

输出：
[null,null,null,null,-3,null,0,-2]

解释：
MinStack minStack = new MinStack();
minStack.push(-2);
minStack.push(0);
minStack.push(-3);
minStack.getMin();   --> 返回 -3.
minStack.pop();
minStack.top();      --> 返回 0.
minStack.getMin();   --> 返回 -2.
```

```java
class MinStack {
    Stack<Integer> stack;
    Stack<Integer> minstack;

    public MinStack() {
        stack = new Stack<Integer>();

        minstack = new Stack<Integer>();
    }
    
    public void push(int val) {//思路：最小栈，是一个从底往上的递减栈
        stack.push(val);

        if (!minstack.isEmpty()){
            int top = minstack.peek();
            if (val <= top){
                minstack.push(val);
            }
        }else{
            minstack.push(val);
        }

    }
    
    public void pop() {
        int pop = stack.pop();

        int top = minstack.peek();

        if (top == pop) {minstack.pop();}

    }
    
    public int top() {
        return stack.peek();

    }
    
    public int getMin() {
        return minstack.peek();

    }
}
```

## 每日温度

给定一个整数数组 `temperatures` ，表示每天的温度，返回一个数组 `answer` ，其中 `answer[i]` 是指对于第 `i` 天，下一个更高温度出现在几天后。如果气温在这之后都不会升高，请在该位置用 `0` 来代替。

**示例 1:**

```
输入: temperatures = [73,74,75,71,69,72,76,73]
输出: [1,1,4,2,1,1,0,0]
```

**示例 2:**

```
输入: temperatures = [30,40,50,60]
输出: [1,1,1,0]
```

**示例 3:**

```
输入: temperatures = [30,60,90]
输出: [1,1,0]
```

```java
class Solution {
    public int[] dailyTemperatures(int[] temperatures) {
        //思路：利用一个储存下标的单调递减栈
        //比当前数小的就会入栈，第一个比当前数大的就会使其出栈
        //等出栈时，使其出栈的下标与栈中的下标的距离就是天数。
        int len = temperatures.length;
        int res[] = new int[len];
        Stack<Integer> stack = new Stack<>();
        stack.push(0);
        for(int i = 1;i < len;i++){
            if (stack.isEmpty()) stack.push(i);
            else{
                if (temperatures[i] <= temperatures[stack.peek()] ){
                    stack.push(i);
                }else{
                    while(!stack.isEmpty() && temperatures[i] > temperatures[stack.peek()]){
                        int cur = stack.pop();
                        res[cur] = i - cur;
                    }
                    stack.push(i);
                }
            }
        }
        return res;
        
    }
}
```

