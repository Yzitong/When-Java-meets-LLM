# 动态规划

**动态规划的核心要素回顾**

动态规划解决问题的核心思路是：

1. **分解问题**：将复杂问题拆解为规模更小的子问题；
2. **存储子问题解**：用某种数据结构记录子问题的解（避免重复计算）；
3. **状态转移**：通过子问题的解推导出更大问题的解（建立递推关系）。

# 爬楼梯

假设你正在爬楼梯。需要 `n` 阶你才能到达楼顶。

每次你可以爬 `1` 或 `2` 个台阶。你有多少种不同的方法可以爬到楼顶呢？

```java
class Solution {
    public int climbStairs(int n) {
        //典型的动态规划，即找出数列的通式与初值
        //爬第n阶楼梯的方法数量，等于 2 部分之和
        //1. 爬上n−1阶楼梯的方法数量。因为再爬1阶就能到第n阶
        //2. 爬上n−2阶楼梯的方法数量，因为再爬2阶就能到第n阶
        //为了让索引位置不越界
        int dp[] = new int[n + 1];
        dp[0] = 1;
        dp[1] = 1;
        for (int i = 2;i<=n; i++){
            dp[i] = dp[i -1] + dp[i - 2];
        }
        return dp[n];

    }
}
```

# 杨辉三角

给定一个非负整数 *`numRows`，*生成「杨辉三角」的前 *`numRows`* 行。

在「杨辉三角」中，每个数是它左上方和右上方的数的和。

```java
class Solution {
    public List<List<Integer>> generate(int numRows) {
        // 用于存储最终结果的列表
        List<List<Integer>> triangle = new ArrayList<>();

        // 初始化第一行，第一行只有一个元素 1
        List<Integer> firstRow = new ArrayList<>();
        firstRow.add(1);
        triangle.add(firstRow);

        // 从第二行开始构建杨辉三角
        for (int i = 1; i < numRows; i++) {
            // 获取上一行
            List<Integer> prevRow = triangle.get(i - 1);
            // 当前行的列表
            List<Integer> currentRow = new ArrayList<>();

            // 每行的第一个元素为 1
            currentRow.add(1);

            // 计算当前行中间的元素，每个元素等于上一行相邻两个元素之和
            for (int j = 1; j < i; j++) {
                currentRow.add(prevRow.get(j - 1) + prevRow.get(j));//语法，在List获取目标索引位置的值
            }

            // 每行的最后一个元素为 1
            currentRow.add(1);

            // 将当前行添加到结果列表中
            triangle.add(currentRow);
        }

        return triangle;
    }
}
```

# 打家劫舍

你是一个专业的小偷，计划偷窃沿街的房屋。每间房内都藏有一定的现金，影响你偷窃的唯一制约因素就是相邻的房屋装有相互连通的防盗系统，**如果两间相邻的房屋在同一晚上被小偷闯入，系统会自动报警**。

给定一个代表每个房屋存放金额的非负整数数组，计算你 **不触动警报装置的情况下** ，一夜之内能够偷窃到的最高金额。

```java
class Solution {
    public int rob(int[] nums) {
        //典型的动态规划题，分类讨论，前n间房子能偷窃到的最高金额是dp[n]
        //对于第 i+1 间房子（即 nums[i]），有两种选择 —— 偷或不偷，取两种选择的最大值
        //可以得到dp[n+1]=max(dp[n],dp[n−1]+num)
        int n = nums.length;
        int dp[] = new int[n + 1];
        dp[0] = 0;
        dp[1] = nums[0];
        for(int i = 1;i < n; i++){
            dp[i + 1] = Math.max(dp[i],dp[i - 1]+ nums[i]);
        }
        return dp[n];
    }
}
```

